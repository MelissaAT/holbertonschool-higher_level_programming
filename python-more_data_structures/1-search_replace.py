#!/usr/bin/python3
def search_replace(my_list, search, replace):
    resutl = re.sub("search", "replace", my_list)
    return (result)
    #for replace, search in enumerate(my_list):
        #if search == my_list[replace]:
            #my_list[replace] = search
    r#eturn(my_list)
